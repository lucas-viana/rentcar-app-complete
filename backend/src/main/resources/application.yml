server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rentcar
    username: postgres
    password: 353742Ap$
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  jackson:
    property-naming-strategy: SNAKE_CASE

jwt:
  secret: rentcar-secret-key-very-long-string-for-jwt-signing-minimum-256-bits-needed
  expiration: 86400000
